## Health
This endpoint is used to check if the database connection is alive. The endpoint starts with `/health.php`.
> ### Health Check
```URL```
```http
GET /
```
```Response```

An empty body with a status code of ```200```.